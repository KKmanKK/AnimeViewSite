import { NextFunction, Request, Response } from "express";
class AnimeController {
  async allAnime(req: Request, res: Response, next: NextFunction) {
    try {
    } catch {}
  }
}

export const animeController = new AnimeController();
